repositories {
  flatDir {
      dirs 'libs'
   }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile(name: 'qmf-ppplugin-android-2.1.1', ext: 'aar')
    compile 'com.android.support:multidex:1.0.0'
}